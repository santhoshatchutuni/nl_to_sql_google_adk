This feature branch is extension to the base code with the below additions

1) adds phoenix integration
2) adds eval integration -- Eval integration just uses llm as a judge . and does not use any adk specific libraries